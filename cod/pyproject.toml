[project]
name = "emotion-analysis-nlp-project"
version = "0.1.0"
description = "End-to-end text emotion analysis with lexicon and learned baselines."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
